# ── OS / editor artifacts ─────────────────────────────────────────────────────
.DS_Store
*.log

# ── Generated / runtime files ─────────────────────────────────────────────────

# Rendered config (generated by the init container at startup).
# Edit config/Config.toml.template instead.
config/Config.toml

# Chainstate and node data written at runtime.
data/

# ── Local environment overrides ───────────────────────────────────────────────

# Copy of .env.example with real credentials — never commit secrets.
.env
